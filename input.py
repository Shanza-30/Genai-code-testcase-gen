# examples/sample_input.py
requirements = """
Create a small Python module that contains:
- a function `is_palindrome(s: str) -> bool` that:
    - returns True for palindromes,
    - ignores punctuation, spaces, and is case-insensitive
- a function `reverse_words(s: str) -> str` that reverses word order but not characters
Include docstrings and simple input validation.
"""
